// bindgen stub for TinyMidiLoader (tml_) — declarations only, no implementation.
// The actual TML_IMPLEMENTATION is compiled into the native tsf library (ext/TinySoundFont/tsf.c).
// tml.h depends on tsf_stream, so tsf.h must be included first.
#include "ext/TinySoundFont/tsf.h"
#include "ext/TinySoundFont/tml.h"
